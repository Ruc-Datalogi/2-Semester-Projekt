public class Graph {
    /*
        The graph class should contain all nodes for a graph
        Travel distance(time) between each nodes
        Time used at each node.
        Location of node?
     */
    //Initilize graph object
    public Graph(int t){

    }
    //Vertex
    /*
        Possible solution:
        each node having a unique ID (key) in a HashMap and then all the data about the given node is stored as the value?
     */
    public void addNode(int x,int y,int stopTime){

    }

}
